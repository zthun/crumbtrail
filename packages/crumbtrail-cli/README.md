# Description

Crumbtrail CLI is a command line application that notifies you when files are
added, updated, or deleted.

This is mostly for testing and finding edge cases when files and folders are not
reporting their system events.

## Installation

```sh
npm i -g @zthun/crumbtrail-cli
```
